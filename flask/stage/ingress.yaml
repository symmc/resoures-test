apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flask-dev
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/whitelist-source-range: "192.168.10.27/32, 178.128.206.69/32"
spec:
  ingressClassName: nginx
  rules:
    - host: app.mine.com
      http:
        paths:
          - path: /api/v.*
            pathType: ImplementationSpecific
            backend:
              service:
                name: flask-dev
                port:
                  number: 80
